# # Model Module

# Custom GPT-2 model implementations with layer-wise attention masking support.

# ## Components
# - `masked_mha.py` - Masked multi-head attention implementation
# - `mask_utils.py` - Attention mask generation utilities  
# - `schedule.py` - Layer-wise masking schedule management
# - `gpt2_custom.py` - Custom GPT-2 with masking support